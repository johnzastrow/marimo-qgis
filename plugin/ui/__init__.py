# marimo-qgis plugin UI / process management.
#
#   process.py — MarimoProcessManager: launches marimo notebooks as detached
#                subprocesses with the bridge connection injected.
#
# (A dock widget, dock.py, arrives in Phase 2.)
